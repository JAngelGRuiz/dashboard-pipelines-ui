const BASE_URL = 'http://localhost:3000'

export const API_ENDPOINTS = {
  jobs: `${BASE_URL}/jobs`,
  pipelines: `${BASE_URL}/pipelines`,
  projects: `${BASE_URL}/projects`,
  traces: `${BASE_URL}/traces`,
  users: `${BASE_URL}/users`
}
